# Dev/offline deps: graph building, extraction, evaluation. NOT installed on the
# server — NFR2 bans torch from the deployed image, and these stay off it.
-r requirements.txt

# google-genai, pydantic and fastembed are runtime deps and pinned in
# requirements.txt; a second `==` here is a double requirement pip refuses.

# Phase A2/A3 — graph build
pandas==2.2.2
numpy==1.26.4

# Testing
pytest==8.3.3

# Phase B — evaluation metrics + TF-IDF baseline
scikit-learn==1.5.2
