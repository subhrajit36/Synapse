# Dev/offline deps: graph building, extraction, evaluation. NOT installed on the
# server — NFR2 bans torch from the deployed image, and these stay off it.
-r requirements.txt

# Phase A1 — LLM extraction
google-genai==1.29.0
pydantic==2.9.2

# Phase A2/A3 — dev-phase embeddings + graph build (replaced by FastEmbed in C1)
sentence-transformers==3.0.1
pandas==2.2.2
numpy==1.26.4

# Testing
pytest==8.3.3

# Phase B — evaluation metrics + TF-IDF baseline
scikit-learn==1.5.2
