# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Data artifacts (keep the repo light; large third-party dumps live locally)
# Re-download with scripts/fetch_onet.sh
data/taxonomies/onet/
*.zip
shreyas_cv.pdf

# Phase C3 ingestion artifacts: checkpoint DBs are machine-local resume state,
# and extractions carry source-document content (PII per the data/raw rule).
data/checkpoints/
data/extractions/
*.sqlite

# Editor
.vscode/
CLAUDE.md
Edge_substrate_plan.md

# Notebooks scratch
.ipynb_checkpoints/

# Env / secrets
.env
venv

# OS
.DS_Store

